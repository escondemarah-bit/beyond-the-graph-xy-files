import {FileText, Palette, QrCode, type LucideIcon} from 'lucide-react'; import {PageHeading,PageShell} from '@/components/page-shell';
const cards: [LucideIcon,string,string][]=[[FileText,'PDF Copy','The official PDF e-portfolio will be available here once an administrator uploads it.'],[Palette,'Canva Version','The online presentation link can be maintained by an administrator.'],[QrCode,'QR Code','A configurable scan destination will appear here once it is set.']];
export default function Archive(){return <PageShell><PageHeading label="The archived copy" title="PDF Copy of E-Portfolio" description="The formal portfolio cabinet. This is separate from the output archive and is managed through the admin records."/><div className="grid gap-5 md:grid-cols-3">{cards.map(([Icon,title,description])=><section className="paper rounded-xl p-6" key={title}><div className="stamp grid size-12 place-items-center"><Icon className="size-5 text-postal"/></div><h2 className="mt-8 font-display text-2xl">{title}</h2><p className="mt-3 min-h-20 text-sm leading-6 text-ink/65">{description}</p><span className="mt-6 block text-sm font-semibold text-ink/45">Not available yet</span></section>)}</div></PageShell>}
