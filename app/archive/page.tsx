import { FileUp, Palette, QrCode } from 'lucide-react';
import {PageHeading,PageShell} from '@/components/page-shell';

const squares=Array.from({length:49});
export default function Archive(){return <PageShell><PageHeading label="Final portfolio cabinet" title="PDF FILE" description="The formal version of our e-portfolio lives here — separately from every member’s personal X-Y folder."/><section className="archive-grid"><div className="space-y-5"><section className="archive-drop"><div><div className="archive-icon mx-auto"><FileUp/></div><p className="eyebrow mt-6">PDF copy of e-portfolio</p><h2 className="mt-2 font-display text-3xl">Reserve this space<br/>for the final file.</h2><p className="mx-auto mt-4 max-w-sm text-sm leading-6 text-ink/65">When the final PDF is added through the group records, its filename, upload date, and open button will appear here.</p><button type="button" disabled className="mt-6 rounded-full bg-postal px-5 py-3 text-sm font-bold text-white opacity-80">Upload PDF · admin only</button></div></section><section className="paper p-6"><div className="flex items-center gap-3"><Palette className="text-orange"/><div><p className="eyebrow">Canva version</p><h2 className="font-display text-2xl">Editable presentation link</h2></div></div><div className="mt-5 flex flex-col gap-3 sm:flex-row"><input aria-label="Canva link" type="url" placeholder="Paste the Canva link in the admin record" className="min-w-0 flex-1 border border-ink/20 bg-white/70 p-3 text-sm"/><button type="button" disabled className="bg-navy px-5 py-3 text-sm font-bold text-white opacity-80">Open Canva</button></div><p className="mt-3 text-xs text-ink/55">No sample link is shown. The group can configure the real Canva destination when it is ready.</p></section></div><aside className="archive-panel"><div className="flex items-center gap-2"><QrCode className="size-5 text-[#ffc67e]"/><p className="eyebrow !text-[#ffc67e]">Scan our portfolio</p></div><h2 className="mt-3 font-display text-4xl leading-none">A BIG QR<br/>CODE SPACE.</h2><div className="qr-stage"><div className="text-center"><div className="qr-grid" aria-label="QR code placeholder">{squares.map((_,i)=><span key={i}/>)}</div><p className="mt-3 text-xs font-bold uppercase tracking-widest">Set destination in group records</p></div></div><p className="config-line">The destination stays configurable—add the final public e-portfolio link once it is ready to share.</p></aside></section></PageShell>}
