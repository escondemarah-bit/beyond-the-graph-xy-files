import {notFound} from 'next/navigation'; import {getTopics} from '@/lib/data'; import {PageHeading,PageShell} from '@/components/page-shell'; import {FileShelf} from '@/components/file-shelf';
export default async function Topic({params}:{params:{slug:string}}){const topic=(await getTopics()).find(t=>t.slug===params.slug);if(!topic)notFound();return <PageShell><PageHeading label="Topic folder" title={topic.title} description="A dedicated record for learning, evidence, and reflection. This file fills as the group adds its actual lectures and work."/><FileShelf type="topic" folderKey={topic.slug} title="ADD FILES" detail="Add lecture PDFs, photos, or videos here. Only this topic folder shows these files—not the Group Feed."/></PageShell>}
