import Link from 'next/link'; import {members} from '@/lib/data'; import {PageHeading,PageShell} from '@/components/page-shell';
export default function Members(){return <PageShell><PageHeading label="The contributors" title="Our Members" description="Five learners, one shared archive. Each profile holds a personal view of the work they choose to document."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{members.map((name,i)=><Link href={`/members/${name.toLowerCase()}`} className="paper rounded-xl p-5 hover:border-postal/50" key={name}><div className="grid size-12 place-items-center rounded-full bg-[#dec7a5] font-display text-xl text-postal">{name[0]}</div><h2 className="mt-8 font-display text-xl">{name}</h2><p className="mt-1 text-sm text-ink/60">Pre-Calculus · Group 1</p><p className="mt-7 text-xs text-postal">Open profile →</p></Link>)}</div></PageShell>}
