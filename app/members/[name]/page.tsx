import {notFound} from 'next/navigation'; import {members} from '@/lib/data'; import {PageShell} from '@/components/page-shell'; import {FileShelf} from '@/components/file-shelf';
export default function Member({params}:{params:{name:string}}){const name=members.find(m=>m.toLowerCase()===params.name.toLowerCase());if(!name)notFound();return <PageShell><section className="relative overflow-hidden border-4 border-postal bg-[#f4deb8] p-7 text-[#271c25] shadow-[8px_8px_0_#dc6c3e] md:p-10"><span className="absolute right-6 top-3 font-display text-7xl text-postal/20">f(x)</span><div className="relative grid size-16 place-items-center rounded-full bg-postal font-display text-3xl text-[#fff4e6]">{name[0]}</div><p className="relative eyebrow mt-6">Personal documentation folder</p><h1 className="relative mt-2 font-display text-5xl">{name}&apos;S X-Y FILE</h1><p className="relative mt-2 max-w-xl text-ink/65">This is separate from the Group Feed. Files added here stay inside {name}&apos;s personal folder.</p></section><section className="mt-10"><FileShelf type="member" folderKey={name.toLowerCase()} title="PHOTOS, PDFs & VIDEOS" detail="Upload this member’s documentation, including videos. These files do not become shared Group Feed posts."/></section></PageShell>}
