import {notFound} from 'next/navigation'; import {members} from '@/lib/data'; import {EmptyState} from '@/components/empty-state'; import {PageShell} from '@/components/page-shell';
export default function Member({params}:{params:{name:string}}){const name=members.find(m=>m.toLowerCase()===params.name.toLowerCase());if(!name)notFound();return <PageShell><section className="paper rounded-2xl p-7 md:p-10"><div className="grid size-16 place-items-center rounded-full bg-[#dec7a5] font-display text-3xl text-postal">{name[0]}</div><p className="eyebrow mt-6">Group 1 profile</p><h1 className="mt-2 font-display text-4xl">{name}</h1><p className="mt-2 text-ink/65">Pre-Calculus · Group 1</p><a href="/login" className="mt-6 inline-block rounded-full bg-postal px-4 py-2 text-sm font-semibold text-white">+ Create Post</a></section><section className="mt-9"><div className="mb-3 flex gap-6 border-b border-ink/15 text-sm"><span className="border-b-2 border-postal pb-3 font-semibold">Posts</span><span className="pb-3">About</span><span className="pb-3">Outputs</span></div><EmptyState title="No posts yet." detail="Your Pre-Calculus journey starts here."/></section></PageShell>}
