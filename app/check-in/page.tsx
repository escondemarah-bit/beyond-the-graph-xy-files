import {PageHeading,PageShell} from '@/components/page-shell';
export default function CheckIn(){return <PageShell><PageHeading label="Private group check-in" title="Kamusta ka na?" description="A weekly, permission-controlled space for each member to reflect. Individual answers are visible only to authorized admins and teachers."/><form className="paper max-w-2xl rounded-xl p-6"><label className="block text-sm font-semibold">Week<label className="sr-only" htmlFor="week">Week</label><input id="week" placeholder="e.g. Week 1" className="mt-2 w-full rounded-lg border border-ink/20 bg-white/60 p-3"/></label><fieldset className="mt-5"><legend className="text-sm font-semibold">How are you doing?</legend><div className="mt-2 grid gap-2 sm:grid-cols-4">{['Doing well','Okay','Struggling','Need help'].map(x=><label className="rounded-lg border border-ink/15 bg-white/50 p-3 text-sm" key={x}><input className="mr-2" type="radio" name="mood"/>{x}</label>)}</div></fieldset>{[['following','Gets/nakakasunod ka pa ba sa lessons natin this week?'],['improve','Ano sa tingin mo yung pwede pa nating iimprove sa group natin?'],['difficult','Kung may aaralin tayo, ano yung pinakamahirap na topic for you?']].map(([id,label])=><label className="mt-5 block text-sm font-semibold" key={id}>{label}<textarea className="mt-2 min-h-24 w-full rounded-lg border border-ink/20 bg-white/60 p-3 font-normal"/></label>)}<a href="/login" className="mt-6 inline-block rounded-full bg-postal px-5 py-3 text-sm font-semibold text-white">Sign in to submit</a></form></PageShell>}
