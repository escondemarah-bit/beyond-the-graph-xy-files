'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {ArrowRight,LockKeyhole} from 'lucide-react'; import {MathHero} from '@/components/math-hero';
export default function Login(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),code:f.get('code')})});setLoading(false);if(!r.ok){setError((await r.json()).error||'Unable to sign in.');return}router.replace('/')}return <main className="math-surface grid min-h-screen place-items-center overflow-hidden bg-[#e8d3b5] p-5"><MathHero/><div className="float-math absolute left-[7%] top-[13%] font-display text-5xl text-postal/40">π</div><div className="float-math-slow absolute bottom-[10%] right-[9%] font-display text-6xl text-navy/35">Σ</div><div className="relative grid w-full max-w-5xl overflow-hidden rounded-[2rem] border border-postal/30 bg-[#fff7e8]/90 shadow-2xl md:grid-cols-[1.1fr_.9fr]"><section className="relative min-h-80 overflow-hidden bg-wine p-8 text-[#f8e6c8] md:p-12"><div className="absolute inset-0 opacity-30"><MathHero/></div><p className="relative eyebrow !text-[#e8bd93]">Group 1 · Pre-Calculus</p><div className="relative mt-14"><p className="font-display text-xl italic text-[#e8bd93]">Beyond the Graph</p><h1 className="mt-2 font-display text-5xl leading-[.85]">THE<br/>X-Y FILES</h1><p className="mt-6 max-w-xs leading-7 text-[#f8e6c8]/80">A digital journey of X, Y &amp; US — functioning together.</p></div><div className="absolute bottom-8 left-8 grid size-16 place-items-center rounded-full border border-[#e8bd93]/60 font-display text-xs uppercase tracking-widest">X · Y<br/>Archive</div></section><section className="p-8 md:p-12"><div className="flex size-11 items-center justify-center rounded-full bg-[#f0d6af] text-postal"><LockKeyhole className="size-5"/></div><p className="eyebrow mt-6">Welcome, researcher</p><h2 className="mt-2 font-display text-3xl">Enter the files</h2><p className="mt-2 text-sm leading-6 text-ink/65">One sign-in opens the full archive for your session.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-semibold">Email<input required type="email" name="email" placeholder="you@email.com" className="mt-1.5 w-full rounded-xl border border-ink/20 bg-white/70 p-3 outline-none focus:ring-2 focus:ring-postal/40"/></label><label className="block text-sm font-semibold">Archive access code<input required type="password" name="code" placeholder="••••••••••" className="mt-1.5 w-full rounded-xl border border-ink/20 bg-white/70 p-3 outline-none focus:ring-2 focus:ring-postal/40"/></label>{error&&<p role="alert" className="rounded-lg bg-[#f6ded6] p-3 text-sm text-seal">{error}</p>}<button disabled={loading} className="group w-full rounded-full bg-postal px-5 py-3.5 text-sm font-semibold text-white shadow-lg transition hover:-translate-y-1 disabled:opacity-50">{loading?'Accessing archive…':'Enter the files'} <ArrowRight className="ml-1 inline size-4 transition group-hover:translate-x-1"/></button></form><p className="mt-6 text-xs text-ink/50">Your access code is securely checked on the server.</p></section></div></main>}
