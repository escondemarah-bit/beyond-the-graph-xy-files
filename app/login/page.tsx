'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {PageShell} from '@/components/page-shell';
export default function Login(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),code:f.get('code')})});setLoading(false);if(!r.ok){setError((await r.json()).error||'Unable to sign in.');return}router.push('/feed')}return <PageShell><section className="mx-auto max-w-md paper rounded-2xl p-7"><p className="eyebrow">Controlled access</p><h1 className="mt-2 font-display text-3xl">Open the archive</h1><p className="mt-3 text-sm leading-6 text-ink/65">Enter your email and the group access code. The code is verified on the server and is never included in browser code.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold">Email<input required type="email" name="email" className="mt-1 w-full rounded-lg border border-ink/20 bg-white/60 p-3"/></label><label className="block text-sm font-semibold">Access code<input required type="password" name="code" className="mt-1 w-full rounded-lg border border-ink/20 bg-white/60 p-3"/></label>{error&&<p role="alert" className="text-sm text-seal">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-postal px-5 py-3 text-sm font-semibold text-white disabled:opacity-50">{loading?'Checking access…':'Continue'}</button></form></section></PageShell>}
