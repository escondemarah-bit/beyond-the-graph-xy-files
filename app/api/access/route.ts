import {NextRequest,NextResponse} from 'next/server'; import {timingSafeEqual} from 'crypto'; import {z} from 'zod';
const bodySchema=z.object({email:z.string().email(),code:z.string().min(1)});
export async function POST(req:NextRequest){const parsed=bodySchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Enter a valid email and access code.'},{status:400});const secret=process.env.ACCESS_CODE_SECRET;if(!secret)return NextResponse.json({error:'Access is not configured yet.'},{status:503});const a=Buffer.from(parsed.data.code),b=Buffer.from(secret);if(a.length!==b.length||!timingSafeEqual(a,b))return NextResponse.json({error:'That access code is not valid.'},{status:401});const response=NextResponse.json({ok:true});response.cookies.set('xy_portfolio_access',parsed.data.email,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',maxAge:60*60*24*7,path:'/'});return response}
