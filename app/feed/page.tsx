import {EmptyState} from '@/components/empty-state'; import {PageHeading,PageShell} from '@/components/page-shell'; import {getTopics} from '@/lib/data';
export default async function Feed(){const topics=await getTopics(); return <PageShell><PageHeading label="The journal" title="Group Feed" description="A shared record of our actual Pre-Calculus journey—posts, photos, videos, questions, and reflections."/><section id="create" className="paper mb-8 rounded-xl p-5"><div className="flex items-center justify-between"><div><h2 className="font-display text-xl">Create a Post</h2><p className="mt-1 text-sm text-ink/65">Sign in to add a record to the journal.</p></div><a className="rounded-full bg-postal px-4 py-2 text-sm font-semibold text-white" href="/login">Sign in</a></div></section><div className="mb-5 flex gap-2 overflow-x-auto pb-1"><span className="rounded-full bg-ink px-4 py-2 text-sm text-white">All</span>{topics.filter(t=>t.parent_id).map(t=><span className="whitespace-nowrap rounded-full border border-ink/20 px-4 py-2 text-sm" key={t.id}>{t.title}</span>)}</div><EmptyState title="No posts yet." detail="Start documenting your Pre-Calculus journey."/></PageShell>}
