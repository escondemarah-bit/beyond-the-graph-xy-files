import {FeedClient} from '@/components/feed-client'; import {PageHeading,PageShell} from '@/components/page-shell'; import {getFeedPosts,getTopics} from '@/lib/data';
export default async function Feed(){const [topics,posts]=await Promise.all([getTopics(),getFeedPosts()]);return <PageShell><PageHeading label="The journal" title="Group Feed" description="The shared living record of our actual Pre-Calculus journey—discoveries, questions, proud moments, and reflections."/><FeedClient topics={topics} posts={posts}/></PageShell>}
