'use client';
import Link from 'next/link'; import { Menu, X, PenLine } from 'lucide-react'; import {useState} from 'react'; import {ThemeToggle} from './theme-toggle';
const links=[['Home','/'],['Topics','/topics'],['Members','/members'],['Media','/media'],['Our Journey','/journey'],['Check-In','/check-in'],["Teacher",'/teacher'],['PDF File','/archive']];
export function SiteHeader(){const [open,setOpen]=useState(false); return <header className="site-header sticky top-0 z-30"><div className="mx-auto flex min-h-16 max-w-7xl items-center justify-between gap-4 px-5"><Link href="/" className="brand-mark"><span>THE</span> X-Y FILES</Link><nav className="hidden items-center gap-4 xl:flex">{links.map(([name,url])=><Link className="nav-link" href={url} key={url}>{name}</Link>)}<Link href="/feed#create" className="create-link"><PenLine className="mr-1 inline size-3"/>Post</Link><ThemeToggle/></nav><div className="flex items-center gap-2 xl:hidden"><ThemeToggle/><button aria-label="Toggle navigation" onClick={()=>setOpen(!open)} className="p-2">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="mobile-nav xl:hidden">{links.map(([name,url])=><Link onClick={()=>setOpen(false)} className="nav-link py-2" href={url} key={url}>{name}</Link>)}<Link onClick={()=>setOpen(false)} href="/feed#create" className="create-link mt-2">Create post</Link></nav>}</header>}
