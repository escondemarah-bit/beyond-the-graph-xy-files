'use client';
import Link from 'next/link'; import { Menu, X, PenLine } from 'lucide-react'; import {useState} from 'react';
const links=[['Home','/'],['Topics','/topics'],['Group Feed','/feed'],['Our Members','/members'],['Media Files','/media'],['Output Archive','/outputs'],['Our Journey','/journey'],['Group Check-In','/check-in'],["Teacher's File",'/teacher'],['PDF Copy','/archive']];
export function SiteHeader(){const [open,setOpen]=useState(false); return <header className="sticky top-0 z-30 border-b border-ink/15 bg-paper/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><Link href="/" className="font-display text-lg text-postal">Beyond the Graph <span className="text-ink">· X-Y Files</span></Link><nav className="hidden items-center gap-5 lg:flex">{links.slice(0,6).map(([name,url])=><Link className="text-sm hover:text-postal" href={url} key={url}>{name}</Link>)}<Link href="/feed#create" className="rounded-full bg-postal px-4 py-2 text-sm font-semibold text-white"><PenLine className="mr-1 inline size-3"/>Create post</Link></nav><button aria-label="Toggle navigation" onClick={()=>setOpen(!open)} className="lg:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="grid border-t border-ink/15 bg-paper px-5 py-4 lg:hidden">{links.map(([name,url])=><Link onClick={()=>setOpen(false)} className="py-2 text-sm" href={url} key={url}>{name}</Link>)}</nav>}</header>}
