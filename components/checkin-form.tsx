'use client';
import {useEffect,useState} from 'react';
const questions=['Kaya pa ba?','Gets/nakakasunod ka pa ba sa lessons natin this week?','Ano sa tingin mo yung pwede pa nating iimprove sa group natin?','Kung may aaralin tayo, ano yung pinakamahirap na topic for you?'];
export function CheckinForm(){const [name,setName]=useState(''),[week,setWeek]=useState(''),[mood,setMood]=useState(''),[answers,setAnswers]=useState<string[]>(['','','','']),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[count,setCount]=useState(0);useEffect(()=>{fetch('/api/checkins').then(r=>r.json()).then(x=>setCount((x.checkins||[]).length)).catch(()=>undefined)},[]);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage('');const r=await fetch('/api/checkins',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({member_name:name,week_label:week,mood,answers:Object.fromEntries(questions.map((q,i)=>[q,answers[i]]))})}),data=await r.json();setBusy(false);if(!r.ok){setMessage(data.error||'Unable to submit.');return}setCount(x=>x+1);setMessage('Check-in submitted and saved for the group.');setAnswers(['','','','']);setMood('')}return <form onSubmit={submit} className="checkin-form"><div className="flex items-center justify-between gap-3"><div><p className="eyebrow">Saved group responses</p><h2 className="mt-1 font-display text-3xl">Start with your mood.</h2></div><span className="text-sm font-bold text-postal">{count} saved</span></div><label className="mt-6 block text-sm font-bold">Your name<input value={name} onChange={e=>setName(e.target.value)} required placeholder="Type your name" className="mt-2 w-full border border-ink/20 bg-white/70 p-3 font-normal"/></label><label className="mt-5 block text-sm font-bold">Week / date<input value={week} onChange={e=>setWeek(e.target.value)} placeholder="e.g. Week 2 · August 15" className="mt-2 w-full border border-ink/20 bg-white/70 p-3 font-normal"/></label><fieldset className="mt-6"><legend className="text-sm font-bold">Kamusta ka na?</legend><div className="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-4">{[['doing_well','😊 Good'],['okay','🙂 Okay'],['struggling','😵 Tired'],['need_help','🆘 Need help']].map(([value,label])=><label className={`mood-card text-sm font-semibold ${mood===value?'ring-2 ring-postal':''}`} key={value}><input className="sr-only" type="radio" checked={mood===value} onChange={()=>setMood(value)}/>{label}</label>)}</div></fieldset>{questions.map((q,i)=><label className="mt-5 block text-sm font-bold" key={q}>{q}<textarea value={answers[i]} onChange={e=>setAnswers(a=>a.map((item,index)=>index===i?e.target.value:item))} className="mt-2 min-h-24 w-full border border-ink/20 bg-white/70 p-3 font-normal" placeholder="Write your answer here…"/></label>)}<button disabled={busy} className="mt-6 rounded-full bg-postal px-6 py-3 text-sm font-bold text-white disabled:opacity-50">{busy?'Submitting…':'Submit check-in'}</button>{message&&<p role="status" className="mt-3 text-sm font-semibold text-postal">{message}</p>}</form>}
